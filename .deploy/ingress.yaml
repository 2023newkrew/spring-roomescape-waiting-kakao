apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: peter-was-ingress
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "false" # HTTPS로의 강제 리다이렉션 방지를 위해 ssl-redirect: "false" 추가
spec:
  rules:
    - host: kane-woz.dev.onkakao.net
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: kane-woz-service
                port:
                  number: 8080
